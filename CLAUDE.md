@AGENTS.md

## Claude-specific

- Use `repo-docs` for repository documentation creation, updates, and audits.
- Use `feature-start` when beginning a new feature from product direction.
- Use `plan-phase` when expanding an accepted implementation milestone into executable steps.
